{
  "name": "agent-router-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that lets Claude Code delegate tasks to Codex as an external subagent.",
  "type": "module",
  "bin": {
    "agent-router-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "start": "node dist/index.js",
    "test": "node test/run-tests.mjs",
    "smoke": "node test/smoke-live.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
